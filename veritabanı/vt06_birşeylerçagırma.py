# use databaseismi:
# SELECT * FROM tabloismi WHERE sütunismi LİKE '%içinde olacak şey%'

# use databaseismi:
# SELECT * FROM tabloismi WHERE sütunismi = 'kayıt'

#PRİMARY KEY ALANI EKLEME
#use databaseismi:
# ALTER TABLE tabloismi ADD COLUMN birtanealanismi İNT PRİMARY KEY auto_increment

# GÜNCELLEME YAPABİLMEK İÇİN
# UPDATE tabloismi SET değiştirileceksütunismi = değiştirelecek şey WHERE değiştirilmeyeceksütun= değiştirilmeyecekşey